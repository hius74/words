# Start Page App

Single Start HTML page for Browser.

## Installation

Make typescript project:

```shell
mkdir learn-app
cd esbuild-html
npm i typescript --save-dev
npx tsc --init
```

Install package to inject JS code into HTML page (run HTML app locally and in Mobile Chrome):

```shell
npm install --save-dev @craftamap/esbuild-plugin-html
```
