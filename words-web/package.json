{
    "scripts": {
        "build": "node --experimental-strip-types --no-warnings build.ts",
        "typecheck": "tsc",
        "format": "npx prettier . --write  --log-level error",
        "lint": "npx tslint src/**/*.ts"
    },
    "devDependencies": {
        "@craftamap/esbuild-plugin-html": "^0.9.0",
        "fake-indexeddb": "^6.2.2",
        "prettier": "3.6.2",
        "typescript": "^5.9.2",
        "vitest": "^3.2.4"
    }
}
